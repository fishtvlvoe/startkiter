# course-quiz-plugin 架構核對

日期：2026-08-24

## 核對結果

- `QuizDefinition` 只透過 `db.pluginContent` 儲存，固定使用 `pluginId: "quiz"`、`type: "quiz-definition"`；沒有新增 Quiz 專屬內容表。
- `QuizAttempt` 是獨立交易型資料表，只對 `User` 建立外鍵；`pluginContentId` 保留為索引欄位，沒有對 `PluginContent` 建立外鍵。
- Quiz plugin 的 `MOUNT_POINTS` entry 只使用 `PluginManifest` 公開欄位：`id`、`name`、`version`、`mount`、`dataSpec`；沒有寫入 Core 保留欄位。
- `mount.content.kind` 為 `auto`、`boundTo` 為 `/quiz`，operator menu route 為 `/quiz-admin`，與 design.md 的 auto-mode 邊界一致。

## 實際驗證

- `pnpm --filter @startkiter/platform test`：9 files、62 tests passed。
- `pnpm --filter @startkiter/database type-check`：exit code 0。
- `git diff --check`：exit code 0。
- `spectra analyze course-quiz-plugin --json`：Coverage／Consistency／Gaps Clean；Ambiguity 僅 1 個 Suggestion，無 Warning/Critical。
- `spectra validate course-quiz-plugin`：exit code 0。

結論：架構核對 Critical 0。
