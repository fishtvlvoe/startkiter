# `course-quiz-plugin` e2e 實跑證據

日期：2026-08-24（Asia/Taipei）

瀏覽器管道：`ego-browser` task space 14，未使用 Playwright／Chrome MCP。

本機服務：重新執行 `prisma generate` 與 migration 後，以全新 `pnpm --filter @startkiter/saas exec next dev --port 3000` 啟動，避免舊 dev process 持有舊 Prisma client。

## Fixture

- Course：`cmt6utlmm0000e6z7iwcitmzx`
- Chapter：`cmt6utlmr0001e6z7dvklzgt1`
- Lesson：`cmt6utlmu0002e6z79po6l78n`
- Lesson 設為 `PUBLISHED` + `isFreePreview=true`，只供本機 e2e 綁定與回收。

## 正向流程：立即顯示答案

1. `/quiz-admin` 實際看到 fixture lesson，建立含單選、多選、是非、填空四題的測驗。
2. 建立的 `PluginContent.id`：`cmt6uwugv0000pmz7o31or2b0`。
3. `/quiz/cmt6uwugv0000pmz7o31or2b0` 實際作答：單選 B、多選 A+C、是、`StartKiter`。
4. 頁面實際結果：`已通過`、`100 分`、`正確答案已顯示`。
5. CLI 實際查詢：`QuizAttempt.score=100`、`passed=true`，`hasPassedQuiz(...)` 回傳 `true`。

畫面：

- [operator 測驗管理頁](./6.3-admin-form.png)
- [學員通過結果頁](./6.3-passed.png)

## 負向流程：不顯示答案

1. `/quiz-admin` 建立 `showAnswers=NEVER` 的四題測驗。
2. 建立的 `PluginContent.id`：`cmt6uxur10002pmz7di81x3fj`。
3. 學員實際送出錯誤答案，頁面結果為 `尚未通過`、`0 分`。
4. 頁面實際顯示 `本測驗不顯示正確答案`，沒有送出正確答案內容。
5. CLI 實際查詢：`QuizAttempt.score=0`、`passed=false`。

畫面：

- [答案隱藏結果頁](./6.3-hidden-answers.png)

## 實際 API 請求

全新 dev server log 實際回傳：

```text
POST /api/rpc/quiz/create 200
GET /quiz/cmt6uwugv0000pmz7o31or2b0 200
POST /api/rpc/quiz/submit 200
POST /api/rpc/quiz/create 200
GET /quiz/cmt6uxur10002pmz7di81x3fj 200
POST /api/rpc/quiz/submit 200
```

## High 修正後重跑（2026-08-24）

修正後重新啟動 `pnpm --filter @startkiter/saas exec next dev --port 3000`，使用 ego-browser task space `startkiter course quiz e2e` 實際走訪：

- learner 登入後開啟既有四題測驗 `cmt6uwugv0000pmz7o31or2b0`，頁面實際呼叫 `POST /api/rpc/quiz/start` 取得伺服器簽章 start token。
- 依序作答單選 B、多選 A+C、是非「是」、填空 `StartKiter`，實際送出後頁面顯示「已通過，得分 100 分」「正確答案已顯示」。
- 開啟 `showAnswers: NEVER` 測驗 `cmt6uxur10002pmz7di81x3fj`，空白送出後頁面顯示「尚未通過，得分 0 分」「本測驗不顯示正確答案」。
- server log 實際出現 `POST /api/rpc/quiz/start 200` 與 `POST /api/rpc/quiz/submit 200`；修正後 submit 不再接受瀏覽器自帶的 `startedAt`。
- DB 實際查詢（本機測試帳號 `cmt6vz57t0000fzz77bd9j49w`）：正向測驗新增 `score: 100, passed: true`，隱藏答案測驗新增 `score: 0, passed: false`；`hasPassedQuiz` 對正向測驗回傳 true。

修正後畫面：

- [修正後通過結果](./6.3-passed-final.png)
- [修正後隱藏答案結果](./6.3-hidden-answers-final.png)
