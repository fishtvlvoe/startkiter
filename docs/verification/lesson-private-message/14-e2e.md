# lesson-private-message E2E

測試環境：`http://localhost:3000`，使用 ego-browser task space `18`，登入帳號 `assignment-operator-e2e-20260824@example.com`；資料庫為本機 PostgreSQL，附件使用 repo 內既有 `docs/design-system-demo/templates/screenshots/course-site-dark.png`，實際大小 114,471 bytes。

## 實跑結果

1. 在學員單元頁 `/course/cmt779upr0003uuz7y5qqsxzo` 的「單元私訊」面板填入訊息並選取附件，畫面回報「私訊已送出」。
2. 瀏覽器實際 PUT 到 signed upload URL，附件 key 為 `cmt779upr0003uuz7y5qqsxzo/3f92ded8-7910-4ebb-8144-cfcf668fc879.png`；key 不含原始檔名。
3. 到 operator 頁 `/lesson-messages`，實際看到同一筆訊息狀態「未讀」，並送出老師回覆「E2E 老師回覆：這個步驟照講義的順序操作即可。」。
4. 實際按「標記已讀」後，operator 頁顯示「已讀」。
5. 回到學員單元頁重新整理，實際看到兩筆訊息：「我」的學員訊息（含 `course-site-dark.png` 附件連結）與「老師」的回覆。

## Database evidence

```text
id                    | content                                      | isFromTeacher | readByTeacher | attachmentName
cmt7jq1fe000386z74nrt42rv | E2E 老師回覆：這個步驟照講義的順序操作即可。 | t             | t             | 
cmt7jpjgl000286z79w16qg86 | E2E UI 私訊：請老師確認這個步驟，附上截圖。  | f             | t             | course-site-dark.png
```

驗收過程中的兩筆探針資料已用精確 ID 清理；上述兩筆完整 E2E 資料保留作為行為驗證證據。

## Final remediation run（2026-08-25）

測試環境：`http://localhost:3001`，ego-browser task space `18`；因本輪使用 3001，啟動時明確設定 `BETTER_AUTH_URL=http://localhost:3001`，避免本機 fallback upload URL 指回 3000。仍使用登入帳號 `assignment-operator-e2e-20260824@example.com` 與同一個 114,471 bytes fixture。

實際操作結果：

1. 學員頁 `/course/cmt779upr0003uuz7y5qqsxzo?message-e2e=final-983ed908-retry` 填入 `E2E final lifecycle retry 983ed908：附件上傳、原子 finalize 與回覆驗收。`，選取 `course-site-dark.png`，畫面實際回報「私訊已送出」。
2. signed upload 完成後，學員重新整理仍看到自己的訊息與「查看附件：course-site-dark.png」連結；資料庫中的 storage key 為 `cmt779upr0003uuz7y5qqsxzo/197482a0-55b0-4829-bdaa-90ddd126be66.png`，不含原始檔名。
3. operator 頁 `/lesson-messages?message-e2e=final-983ed908-retry` 實際看到該筆訊息狀態「未讀」，送出 `E2E final lifecycle 回覆：附件已先驗證、intent 僅能 finalize 一次。`，畫面回報「老師回覆已送出」。
4. 實際按「標記已讀」，operator 畫面回報「私訊已標記為已讀」，該卡片狀態變成「已讀」。
5. 回到學員頁重新整理，實際看到「我」的附件訊息與「老師」的回覆。

Database evidence：

```json
[
  {"id":"cmt7lmnfq0001wiz7afd3x3qi","content":"E2E final lifecycle retry 983ed908：附件上傳、原子 finalize 與回覆驗收。","isFromTeacher":false,"readByTeacher":true,"attachmentName":"course-site-dark.png","attachmentStorageKey":"cmt779upr0003uuz7y5qqsxzo/197482a0-55b0-4829-bdaa-90ddd126be66.png","attachmentSize":114471},
  {"id":"cmt7lnqxw0002wiz7jsp88dho","content":"E2E final lifecycle 回覆：附件已先驗證、intent 僅能 finalize 一次。","isFromTeacher":true,"readByTeacher":true,"attachmentName":null,"attachmentStorageKey":null,"attachmentSize":null}
]
```

同一輪 intent 查詢結果為 `id=cmt7lmn9w0000wiz7y0byhnt0`、`status=FINALIZED`，storage key 與訊息一致。瀏覽器 console 仍有既有的 watermark hydration mismatch 與 comments 403，兩者與本 change 私訊流程無關；私訊 API、上傳、回覆、已讀與重新載入均實際成功。

ego-browser `captureScreenshot` 在本機 CDP `Page.captureScreenshot` 逾時，未把失敗的截圖呼叫冒充成證據；可直接在 task space `18` 查看目前完成的 operator 頁面，且 final handoff 會保留該頁開啟。
