# i18n Scenario 驗證

驗證日期：2026-08-18

## Scenario 結果

| Scenario | 結果 | 證據 |
| --- | --- | --- |
| `/zh-tw` homepage renders | PASS | marketing dev server GET `/zh-tw` HTTP 200，頁面含「已經在用 AI 做事的人」與「一次買斷，帶走課與終身代碼包」 |
| `/zh-cn` homepage renders | PASS | GET `/zh-cn` HTTP 200，頁面含「已经在用 AI 做事的人」與「一次买断，带走课程与终身代码包」 |
| `/en` homepage renders | PASS | GET `/en` HTTP 200，頁面含「Build your SaaS without rebuilding the foundations」 |
| Locale list is extensible through package config | PASS | locale routing 由 `packages/i18n/config.ts` 的 `locales` 與 `apps/marketing/modules/i18n/routing.ts` 共用，頁面元件未為三語系各自分支 |
| Missing English key falls back to zh-TW | PASS | `i18n.test.ts` 刻意使用只存在 zh-TW 的 `home.legacyFallback`；`getMessagesForLocale("en", "marketing")` 回傳繁中值，不回傳 literal key |

## 指令結果

```text
pnpm --filter @startkiter/i18n exec vitest run packages/i18n/i18n.test.ts

Test Files  1 passed (1)
Tests       8 passed (8)
```

```text
GET /zh-tw 200
GET /zh-cn 200
GET /en    200
```
