// Compatibility import path for existing API consumers. The canonical resolver
// lives in @startkiter/course so Studio and the learner player share one policy.
export { resolveVideoSource } from "@startkiter/course/video-resolver";
export type { ResolvedVideoSource } from "@startkiter/course/video-resolver";
